import SwiftUI

/// App-wide semantic colors. Accent matches the work-phase green in `PhaseColors`.
enum AppColors {
    static var accent: Color {
        Color.accentColor
    }

    static let destructive = Color.red
}
